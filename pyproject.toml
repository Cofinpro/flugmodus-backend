[project]
name = "flugmodus-backend"
version = "0.1.0"
description = "'Secure' offline payments and 'settlement'. Cofinpro Coding Dojo 2026 :)"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "fastapi[standard]>=0.141.1",
]

[tool.fastapi]
entrypoint = "main:app"
[dependency-groups]
dev = [
    "pre-commit>=4.6.2",
]
